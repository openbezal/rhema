import type { Step } from "react-joyride"

export const TUTORIAL_STEPS: Step[] = [
  {
    target: '[data-slot="transport-bar"]',
    content: "Welcome to Rhema! This is your control bar.",
    placement: "top",
    disableBeacon: true,
  },
]
