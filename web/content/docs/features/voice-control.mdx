---
title: Voice Control
description: Hands-free translation switching and chapter navigation while you're preaching.
icon: Mic2
---

import { Callout } from "fumadocs-ui/components/callout";

Rhema listens for a small set of voice commands so you don't have to
break flow at the lectern. Commands are recognized inline with the
sermon transcript — there's no wake word and no separate listening mode.

## Translation switching

The translation switcher activates whenever the detector hears one of
these patterns:

- "read in **NIV**"
- "read in the **NLT**"
- "switch to **ESV**"
- "let's hear it in **NASB**"
- "in the **King James**"

Translation aliases are forgiving — "King James", "KJV", and "Authorized
Version" all map to the same translation. Once recognized, the active
translation flips immediately and the live overlay reflows.

## Chapter navigation in reading mode

When the pastor announces a passage to read through ("Let's turn to
**Romans 8**"), Rhema enters reading mode and the queue locks to that
chapter. Voice commands then advance through the text:

- "**next chapter**" / "**previous chapter**"
- "**chapter 5**"
- "**verse 12**"
- "**back two verses**"
- "**exit reading mode**"

This is much faster than fishing for verses in the queue while preaching.

<Callout title="False-positive guard" type="info">
Voice commands only fire when the detector is confident the words are a
command, not part of the message. "Next chapter" said as a teaching point
("the next chapter of your story") won't trigger navigation.
</Callout>

## Disabling voice control

If your service style doesn't suit voice commands — or if a guest speaker
keeps tripping them accidentally — you can turn voice control off in
Settings → Detection. Translation switching and chapter navigation then
fall back to UI/remote control only.

## Detection guards

Voice commands are protected by:

- A **dedup guard** in `check_translation_command` so a command isn't
  fired twice from the same transcript window.
- **Sermon context** awareness — commands during reading mode prefer
  navigation patterns; commands outside reading mode prefer translation
  switching.
- A short cooldown after each command so a rapid back-to-back utterance
  ("read in NIV — actually NLT") only triggers the latest one.
